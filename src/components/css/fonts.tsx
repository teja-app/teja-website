
import { Lato, Signika_Negative } from 'next/font/google';

export const titleFontCss = Signika_Negative({
    weight: '500',
    subsets: ['latin'],
})

export const bodyCss = Lato({
    weight: '400',
    subsets: ['latin'],
});

export const headingFontCss = Lato({
    weight: '700',
    subsets: ['latin'],
});
